# 게임시스템 명세 — v1.3

> 상태: **현행** · 최종 갱신: 2026-07-13 · 상위 기준: [vision.md](vision.md)
> 대상: `web/` 프로토 (커밋 `7fdd4ae` = PR #3 머지) · Unity 포팅(`unity/`)은 동일 로직의 C# 미러
> 이 문서는 **현재 구현된 시스템의 전체 명세**다. 수치의 원본은 언제나 코드
> (`web/src/data/*.ts`)이고, 여기 적힌 값은 그 스냅샷이다. 값이 다르면 코드가 맞다.
> 미확정 아이디어는 [backlog.md](backlog.md)에 있다 — 여기엔 결정된 것만.

---

## 0. 문서 지도

전체 문서 목록과 상태는 **[docs/README.md](../README.md)**에 있다. 이 명세는
[vision.md](vision.md)(비전·필러)를 상위 기준으로 두고, 근거는
[../reports/](../reports/)의 측정 리포트가 받친다.

수치 표기: **[원본확정]** = 맵파일에서 직접 읽음. **[프로토]** = 원본이 EUD로 가려져
읽을 수 없어 플레이 가능하게 정한 값.

---

## 0.5 코어 루프 — 플레이어가 반복하는 경험

```
웨이브 예측 → 유닛 생성·조합 → 경제 배분 → 전투 관찰 → 보스 소환 판단
     → 증강·레벨 보상 → 빌드 강화 → 다음 웨이브 (22초 주기)
```

| 단계 | 보는 정보 | 내리는 결정 | 비용 | 즉시 피드백 |
|---|---|---|---|---|
| 웨이브 예측 | 라운드 번호, 사이클 위치(5R 주기), 몹 장갑 계단 | 티어를 올릴 때인가 | — | 다음 웨이브의 체감 난이도 |
| 유닛 생성·조합 | 빈 타일, 보유 유닛 구성, **현재 생성가** | 어디에 몇 기 뽑나 | 미네랄 12+ (누적 증가 §3) | 랜덤 유닛 등장, 조합 시 상위 티어 팝 |
| 경제 배분 | 미네랄/가스 잔고, 프로브 수 | 유닛 vs 프로브 vs **영웅 XP** / 업글 vs 리롤 vs 스킬 개조 | 미네랄·가스 | 버튼 비용 갱신, 가스 수급률 |
| 전투 관찰 | 몹 진행, 영웅 어그로, 타워 화력 | 영웅을 어디 세울까 | — | 피해 숫자, 누출 경고 |
| 보스 소환 | 쿨타임, 열린 레벨, 현재 화력 | 지금 걸어볼 만한가 (저지 불가 리스크) | 실패 시 라이프 2+Lv | 처치 보상 or 누출 |
| 증강 보상 | 카드 3장, 보유 계열, 등급 | 몰 것인가 넓힐 것인가, 리롤할 것인가 | 리롤 가스 | 시너지 발동, 빌드 방향 확정 |

실패에서 배워야 하는 것: 누출 위치(화력 공백 구간), 보스 실패(소환 타이밍 오판),
영웅 사망(스탯 대비 과한 어그로).

## 1. 맵과 경로

- **십자(cross) 지형**: 중앙 제단 타일 1 + 팔 4방향. 타워 타일 **41개**
  (십자 17 = 중앙 1 + 팔당 4 × 4방향, 모서리 4구역 × 6).
- **몹 경로**: 북측 왼쪽 입구에서 스폰 → 십자 외곽선을 **반시계** 일주 → 북측 오른쪽
  출구. 웨이포인트 14개, 경로 위 위치는 좌표가 아니라 "일주 거리" 스칼라 하나로 표현.
- **제단**: 게임 시작과 함께 십자 중앙에 무료 제공. 그 타일에는 타워 불가.
- 영웅과 몹은 경로 위로만 다닌다. 빈 곳 클릭 시 최근접 경로점으로 투영 이동.

## 2. 라운드와 웨이브

| 항목 | 값 | 출처 |
|---|---|---|
| 라운드 간격 | **22초 고정** (원본 57초) | [프로토] / [원본확정] |
| 첫 라운드 대기 | 5초 (원본 20초) | [프로토] |
| 웨이브 몹 수 | 20 + 4×(사이클 내 위치 0~4) = 20→36 | [프로토] |
| 스폰 간격 | 0.18초 (36기 × 0.18 = 6.5초 스폰 창) | [프로토] |
| 몹 이동속도 | **42** (52 → 42, 2026-07-14 — 초반 템포 완화) | [프로토] |
| 초반 템포 | `earlyTempo(round)` = R1 0.6 → R5 1.0 램프. **전투만 슬로우**(라운드 타이머·스폰은 실시간 유지) | [프로토] |
| 라운드 수 | 현재 무한 — **유한 종결 R60 설계 확정, 미구현** (§12, [설계](../exec-plans/finite-ending-design.md)) | [프로토] |

**고정 간격인 이유**: 웨이브를 빨리 정리해도 다음 라운드가 앞당겨지지 않는다.
클리어 시 진행 방식이면 천천히 잡을수록 시간당 보스 소환 횟수가 늘어나는 악용이 생긴다.

### 2.1 난이도 곡선 — 재화→전투력 모델 역산 (2026-07-11 재설계)

```
웨이브 총체력 = 기대유효DPS(라운드) × 목표clear(라운드)
몹 1기 체력  = 총체력 ÷ 몹 수         (사이클 경계에서 수가 리셋 → 개당 체력 점프)
기대유효DPS  = 시뮬 측정(P50, 장갑 감산 반영)의 구분지수 근사
목표clear    = R1 18초 → R44 37초 (선형) → 이후 ×1.2/라운드 (지수 벽)
몹 장갑      = floor(라운드/5) × 3     (5라운드 계단 — 유지)
몹 공격력    = 라운드에 선형 (근접 접촉, 영웅·허수아비만 때린다)
```

- 이전의 "사이클마다 HP ×2 점프"는 폐기 — 사이클 안 평평한 HP를 초반 ×1.2/R로 크는
  플레이어 DPS가 앞질러 R30까지 난이도가 체감되지 않았다(플레이테스트 + 위험률 0%).
- **벽이 지수인 이유**: 선형 벽은 상대 성장률이 감소해 지수로 크는 보드가 결국 이긴다
  (측정: 봇이 R138까지 생존).
- **발견 — 압박→성장 되먹임**: 웨이브가 세지면 몹이 오래 살고 영웅 막타 XP가 늘어
  성장이 가속된다. DPS/수입 비율이 1.5→2.5로 오른다. 곡선은 이 되먹임 반영 재측정값.
- 사이클 리듬은 유지: 몹 수가 사이클 안에서 늘고(20→36), 경계에서 리셋되며 개당 체력이
  뛴다 — "새 몹은 적지만 굵다".
- **누출**: 몹이 출구에 도달하면 라이프 -1, 미네랄 +5 [원본확정]. 시작 라이프 20.
- 봇 기준 사망 중앙값 R60, 분포 R55~66 (시드 24) — R60 유한 종결과 정합.

## 3. 유닛(타워)과 조합

- **생성**: `12 + 0.45×max(0, n−8)` 미네랄 (n = 누적 생성 횟수 — 조합해도 리셋 안 됨),
  랜덤 유닛 (4종족 × 티어 풀, 7종 균등) [프로토 — 원본 기본가 12는 strings:412].
  고정 12는 GOD 20기·타일 포화를 만들었다(§11).
- **조합**: 같은 이름 2기가 모이면 **자동으로** 다음 티어 랜덤 유닛 1기로 합성.
  연쇄 조합 지원. 이것이 이 게임의 기저 도파민이다(판당 ~69회).
- **티어**: 5단계 (Lv1~4 + **GOD**). 티어 피해 배수 [1, 3, 9, 28, 95] × 기본 7.
  사거리 [120, 140, 160, 185, 225]. 기본 공격 간격 0.9초.
- **태그**: power(피해 1.6×, 사거리 1.15×, 보스 우선 타겟) / splash(피해 0.7×, 광역) /
  speed(간격 0.5×).
- **크리쳐 종족**: 보조 타워 — 피해 0.6×이지만 사거리 안 몹 감속.
  감속은 **가장 강한 것 하나만** 적용(도배 방지). 티어별 [0.9, 0.84, 0.76, 0.66, 0.5].
- **판매** 가능. GOD 풀은 보스 처치 수에 따라 조기/확장 분기.

## 4. 경제 — 재화마다 싱크가 경쟁한다

```
미네랄 → 유닛(12+0.45×(n−8)) / 프로브(100×1.5ⁿ, 최대 16기) / 영웅 XP(20골드=20XP)
가스   → 종족 업그레이드(8+4n+n²) / 증강 리롤(12→24, 최대 2회) / 스킬 개조(30×1.35ⁿ)
```

라운드별 수입 저점·고점과 몬스터 압력·기대 화력의 공통 기준표:
**[income-curve.csv](../balance/income-curve.csv)** (`gen:balance` 산출).

**설계 원칙**: 싱크가 하나뿐인 재화는 자동 소비가 되어 선택이 사라진다.
(개편 전 GA가 전 세대 "프로브 8 + 가스 즉시 업글"로 수렴했던 이유.)

### 4.1 소득 [원본확정 중심]

| 소득원 | 값 |
|---|---|
| 시작 자원 | 미네랄 55 · 가스 6 |
| 웨이브 클리어 | 10 + 3×라운드 [프로토 — 원본은 반복 20킬 보상] |
| 보스 처치 | Lv1~6: 5 / 10 / 18 / 32 / 55 / 90 [프로토 — 원본 5/8/13/20/29/39는 리스크 추종 위해 대체] |
| 킬 마일스톤 | 200킬 간격 20단계 (+5~15) |
| 누출 위로금 | +5 |
| 가스 | 프로브 1기당 0.25/초 |

### 4.2 프로브 딜레마

비용 100×1.5ⁿ — 첫 기부터 시작 미네랄(55)로 못 산다(오프닝 구매 봉인).
30×1.3ⁿ 시절엔 "무조건 프로브 1순위"의 지배 전략이었다(생존 우위 +8R, 초반 리스크 0).
측정(100/1.5): 타워 올인 중앙값 R58 vs 프로브 전략 R60 — 우위 +2R로 압축, 선택 성립.

### 4.3 종족 업그레이드

가스로 종족별 공격력 +10% 복리. 비용 8+4n+n². 타워 화력의 후반 축.

## 5. 보스 — 상시 소환, 리스크 있는 결정

- **소환**: 라운드와 무관하게 언제든, 쿨타임 45초, 비용 0.
- **해금**: Lv N 처치 → Lv N+1 개방. 열린 레벨은 계속 재소환 가능 (낮은 레벨 = 안전하지만 보상 적음).
- **HP**: 1150 × 2.5^(Lv-1) · **장갑**: 3×Lv · 속도 26.
- **Lv3까지는 영웅·허수아비를 공격하지 않고 지나간다** — 저레벨 보스는 소득원이고,
  위협은 못 잡았을 때의 누출 라이프뿐. Lv4부터 잡몹 여러 기 몫으로 때린다.
- **보스는 영웅에게 저지되지 않는다** — 화력이 모자라면 일주를 마치고
  **라이프 2+Lv를 문다**. 도발 인형(허수아비 개조)만 보스를 붙잡을 수 있다.
- **레벨 사다리에 리스크가 있다** (항상 최고 소환 봇 측정): 처치율 Lv3 91% ·
  Lv4 73% · Lv5 54% · Lv6 75% — "항상 최고 소환 = 항상 승리"(구 96%)를 깼다.
- **Lv1 기준선** (테스트로 고정): 시작 미네랄 55(유닛 4기)로는 **2단계 조합이 떠야**
  잡는 판이고, 75(6기)면 거의 잡는다(≥85% — 웨이브 밀도 상향 후 완화된 문턱).
- 보스 처치는 영웅 XP의 페달이기도 하다 (xpPerBoss).

## 6. 영웅 — 골드→XP→자동 균등 성장 (3안, 2026-07-13)

```
영웅 파워 = 스탯(레벨업 자동 균등 성장) × 증강 배수(선택)
```

레벨 배수는 **폐지**됐다 — 골드 없이 XP만으로 오르던 ×2.4/레벨이 파워커브 과속
(Lv17 DPS 985 = Lv3 타워의 6배)의 원인이었다. 이제 영웅 파워는 전부 골드 비용을
가지므로 [income-curve.csv](../balance/income-curve.csv)에서 타워와 같은 저울에 오른다.

### 6.1 성장 한 축 — XP·레벨·스탯

- **XP는 골드로 산다**: 1골드 = 1XP, 버튼당 20 (TFT식). **성장의 주 연료.**
  킬 XP(0.3/기, 영웅 막타 ×2, 보스 8×Lv)는 부축이다.
- **레벨업 = 세 스탯 자동 균등 성장**: 레벨당 총 `2 + floor(L/10)`포인트를
  힘·민첩·지능에 1/3씩 나눈다. 소수 성장치를 허용해 기존 총 포인트 예산을 보존한다.
- 레벨업 스탯 선택과 선택 대기 UI는 없다. 빌드 방향 전환은 증강 선택으로 담당한다.
- 레벨업 비용: 14 × 1.06^L 지수 — 실질 상한 Lv60대 (R60 고점 수입으로 Lv65 불가).
- 레벨업 시 완전 회복. 죽어 있으면 XP 구매 불가.

**기본 체력 260** (레벨 무관, 2026-07-14 신설). 체력이 `힘×70`뿐일 때 Lv1 영웅이 140이었고,
어그로 110에 몹이 10기씩 뭉쳐 붙어 **R1에 8.8초면 죽었다**(라운드는 22초). 측정: 시드 12판 중
9판에서 영웅 사망, 첫 사망 중앙값 R5. 기본값은 **초반만** 두껍게 한다 — 레벨이 오를수록
힘 몫이 커져 비중이 줄므로 후반 탱커 빌드의 존재 이유는 그대로다.

| 스탯 | 포인트당 효과 | 기본값 |
|---|---|---|
| 힘 | 공격력 +6 · 체력 +70 | 2 |
| 민첩 | 공격 속도 +4% (간격 하한 0.25초) | 8 |
| 지능 | 스킬 피해 +3.5% | 8 |

### 6.2 밸런스 앵커 (테스트 고정 — `hero-curve.test.ts`)

- **골드 저울은 재측정 대기**: 2안의 Lv27 힘몰빵 DPS 앵커는 자동 균등 성장으로 무효화했다.
  시스템 확정 후 영웅의 피해·탱킹·스킬 기여를 함께 측정해 새 기준을 정한다.
- **레벨 속도**: 킬 XP만으론 R45에 Lv25 미달 / 저점 수입 20%를 XP에 넣으면 Lv25~31 —
  성장 정지선(R40~45)과 정합.
- **증강이 역전 담당**: 무증강 Lv30 < GOD 1.5기 · 공격 3증강(Lv24) = GOD 1~2.5기.
- 초반 가드: Lv1 DPS < GOD 5%, Lv8 < GOD 25%.

### 6.3 이동·어그로·부활

- 영웅은 경로 위로만 이동(속도 88). 어그로 범위 110 — 사거리 안 몹은 영웅 앞에
  멈춰 싸운다(모으기+지연). **보스는 예외**(§5).
- 붙잡힌 몹에게 타워가 넣는 피해는 `tankAssistDamage`로 집계 — 탱킹의 가치가 숫자로 보인다.
- 사망 시 12초 후 제단에서 부활(증강으로 단축 가능).

## 7. 증강 — 적응형 드래프트

영웅 타입/전직은 **없다**. 빌드 방향은 드래프트의 관성이 만든다.

- **스케줄**: 영웅 레벨 [9, 16, 24, 30, 35, 42], 이후 8레벨마다. 선택 중 게임 일시정지.
  80/20 — 앞의 4개는 거의 모든 판이 받고(진행률 절반 내), 5·6번째는 잘 버틴 판의 보상.
- **선택지**: **87종** 풀에서 3장. **가중치 = 1 + 0.9 × (그 계열 보유 수)** — 몰수록 그
  계열이 더 잘 뜬다. 강제가 아니라 관성: 첫 장부터 특화해도, 범용부터 집어도 된다.
- **계열 7종 — 기능축이다.** 계열 = 그 증강이 게임에 무엇을 더하는가.
  그래서 특화가 곧 플레이 스타일이 된다.

| 계열 | 하는 일 | 대표 |
|---|---|---|
| 강화 | 순수 수치 | 완력, 명사수, 광전사 |
| 방어 | 생존 — 피해 감소·재생·흡혈·가시 | 중장갑, 흡혈, 가시 갑옷 |
| 특수 | 새 발동 효과 — 치명타·처형·화상·감속·폭사 | 처형, 화염 부착, 폭사 |
| 스킬 | 액티브 스킬 획득과 개조 | 유성, 연사, 과부하 |
| 성장 | 누적 — 막타·라운드마다 영구히 커진다 | 사냥 본능, 역전의 용사, 진화 |
| 경제 | 수입 — 미네랄·가스·경험치 | 탐욕, 가스 정맥, 학자 |
| 유틸 | 기동·부활·타워 지휘·어그로·**타워 복제** | 전쟁군주, 도발, 복제 장치 |

**증강은 가산으로 쌓인다.** `공격력 +60%` 세 장은 +180%(×2.8)지 1.6³(×4.1)이 아니다.
시너지도 가산이다. 전부 곱이면 스택형 증강이 복리로 터진다 — 측정에서 성장 3장이
GOD 타워 **14.7기**(R44 보드 전체가 5.1기)까지 갔다.

**곱연산은 셋뿐이다** (`COMPOUNDING_IDS`): 광전사(최종 공격력 ×1.5) · 진화(성장 누적치 ×1.4) ·
과부하(스킬 피해 ×1.6). 곱이 흔해지면 다시 복리가 되므로, 소수만 두고 배율은 낮게 잡는다.
그래야 곱이 **사건**이 된다.

- **등급**: 실버 1.0× (55%) / 골드 2.0× (33%) / 플래티넘 3.5× (12%). **대가 없음** —
  순수 뽑기 운. 높은 등급이 뜬 순간이 그 판의 도파민이다.
- **대가형(리스크) 증강**: 12종은 이득에 **대가**가 달린다 (예: 광전사 — 공격력 +80%,
  최대 체력 -30%). **등급은 이득만 키우고 대가는 키우지 않는다** — 대가가 등급에 비례하면
  높은 등급이 뜬 게 손해인 경우가 생겨 "등급은 순수 운" 원칙이 깨진다.
- **리롤**: 가스 12(2회째 24), **선택당 최대 2회**.
- **시너지**: 같은 계열 3개 = 특화, 5개 = 대특화. 후반 역전은 레벨이 아니라 여기서 나온다.
  성장 대특화(무한 성장)는 누적치를 +150%, 경제 대특화(대부호)는 라운드마다 미네랄 12·가스 3.
- 제약: 스택 상한 / 대폭발은 충격파(광역) 필요 / 스킬은 하나만 / 스킬 개조는 보유 스킬만.
- 성장 스택은 **영웅 막타만** 센다(타워가 잡은 몹은 안 센다). 처형은 **보스에 걸리지 않는다**.
  폭사는 연쇄하지 않는다 — 폭발이 죽인 적은 다시 터지지 않는다.
- **화상은 평타로 안 붙는다 — 스킬과 도트(레이저 틱·불바다 장판)만 불을 붙인다.**
  중첩 상한이 없고, 겹당 피해는 **공격력 계수 없는 고정값**이며 **방어력을 무시한다(트루 피해)**.
  그래서 화염의 성장축은 "얼마나 세게"가 아니라 **"얼마나 여러 겹"**이다 —
  마나 회전이 빠를수록, 도트 틱이 잦을수록 두꺼워진다. 점화는 폭발이 아니라 **화염 피해 배수**다.
- 맹독은 딜이 아니라 **약화**다 — 감속 + **방어력 감소**(타워 피해도 함께 이득).

## 8. 액티브 스킬 — 자동 시전

플레이어는 **무엇을 들고 어떻게 개조할지만** 고른다. 시전은 조건 충족 시 자동.

| 스킬 | 원형 | 시전 | 효과 | 마나 | 자동 시전 조건 |
|---|---|---|---|---|---|
| 소용돌이 | 근접 | 단발 | 주변 전체에 공격력 3× | 100 | 2기 이상 |
| 처형자의 일격 | 근접 | 단발 | 체력 최저 적에 8× — **처치 시 마나 소모 없음** | 110 | 1기 이상 |
| 일제 사격 | 원거리 | 단발 | 사거리 안 4명에 각 2× | 90 | 1기 이상 |
| 튕기는 사격 | 원거리 | 단발 | 튕길 때마다 ×1.3 (5회) — **몹이 많을 때만 강하다** | 165 | 3기 이상 |
| 불화살 | 원거리 | 단발 | 바닥에 **불바다 장판** (초당 1.2×, 6초) | 150 | 2기 이상 |
| 얼음화살 | 원거리 | 단발 | 바닥에 **빙판 장판** (55% 감속, 7초) | 175 | 3기 이상 |
| 유성 | 마법 | 단발 | 최다 밀집점에 6× 광역 | 160 | 3기 이상 |
| 레이저 | 마법 | **채널링** | 앞쪽 직선 관통 지속 빔 (0.5초마다 0.55×, 3초) | 160 | 2기 이상 |
| 허수아비 | 탱커 | **채널링** | 앞에 미끼 설치 (영웅 체력의 60%, 9초) | 220 | 앞쪽에 몹 |

### 8.1 마나 — 쿨타임은 없다 (TFT식, 2026-07-14)

스킬은 **마나가 차면 자동으로 나간다.** 마나는 **평타를 칠 때(+10)와 맞을 때(+8)** 찬다.

- **공격 속도가 곧 스킬 회전이다.** 민첩에 투자하면 평타도 스킬도 빨라진다.
- **탱커도 스킬을 쓴다.** 맞으면 마나가 차므로 어그로를 끄는 것 자체가 자원이 된다.
  (`반격 집중` — 피격 시 마나 +12.)
- **채널링은 지속 중 마나가 차지 않는다.** 그래서 가동률에 천장이 생긴다 —
  레이저는 빔 3초 동안 마나가 멎고, 끝난 뒤부터 다시 찬다.
  단발은 마나가 차는 대로 계속 터진다. 그래서 "마나 증강을 몇 장 집을 것인가"의 답이
  스킬마다 다르다.
- 필요 마나 감소에는 바닥이 있다(원래 값의 40%). 없으면 '난사' 빌드가 같은 장수의
  강화 빌드를 10~40배로 압도했다 (측정 후 하향).

- **개조 2트랙**: 증강(질 — 폭발 화살, 연사, 도발 인형 등 질적 변화) +
  가스 구매(양 — 피해 +8%/회, 필요 마나 -6%/회, 비용 30×1.35ⁿ).
- **공용 개조**는 모든 스킬에 걸린다: 한파(스킬에 감속 추가) · 다중 투사(대상 +2, 튕김 +2) ·
  반격 집중(피격 시 마나 +12) · 마나 순환(획득 +18%) · 오버클럭(필요 마나 -18%, 피해 -12%) ·
  축전지(시전 후 마나 25 잔류).
- **레이저 개조**: 집속 렌즈(두께) · 증폭 코일(길이·지속) · 고속 조사(도트 간격 0.5→0.25초).
  **장판 개조**: 넓은 화선(반경·지속) · 맹렬한 불길(초당 피해).

## 9. 점수와 명예의 전당

- 라운드 통과: 60 × 1.15^라운드 (지수 — 사망 라운드 차이가 점수 격차로 증폭된다).
- 킬 10 / 영웅 레벨업 300 / GOD 타워 5,000 / 누출 시 라운드 점수의 25% 감점.
- 무한 라운드 + 로컬 명예의 전당(웹).

## 10. 검증된 밸런스 스냅샷 (2026-07-11 개편 후 · 봇 시뮬)

| 지표 | 측정값 |
|---|---|
| 사망 라운드 | 중앙값 **R60** · 분포 R55~66 (시드 24) |
| 초반 압박 | R1 누출 3.5 · R3까지 라이프 20→15.8 · 몹 동시 17~28기 |
| GOD 타워 | 1기 R28 · 5기 R55 · R51 시점 4.5기 (개편 전 20기) · 타일 포화 소멸 |
| 보스 처치율 (항상 최고 소환) | Lv3 91% · Lv4 73% · Lv5 54% · Lv6 75% |
| 프로브 전략 | 타워 올인 R58 vs 프로브 R60 — 우위 +2R (개편 전 +8R·리스크 0) |
| 재화→전투력 | 유효 DPS ≈ 누적수입 ×1.5~2.5 · 운 스프레드 P10 0.65× / P90 1.8× |

> **재측정 필요**: 빌드별 영웅몫·생존 스프레드·탱커 어시스트는 영웅 2안 개편(§6) 이후
> 아직 재지 않았다. 구 수치(마법사 43~48% 등)는 폐기 — 인용 금지.

## 10.5 목표 지표 — 측정값의 성공/실패 판정 기준

| 지표 | 목표 | 현재 | 강제 수단 |
|---|---|---|---|
| Lv1 보스 처치율 (75골드) | 거의 잡는다 | ≥85% | `tests/boss-balance.test.ts` |
| 골드 저울 (영웅 vs 타워) | 같은 골드 한계 DPS ±60% | 통과 | `tests/hero-curve.test.ts` |
| 영웅 레벨 속도 | R45에 Lv25~31 (수입 20% 투자 기준) | 통과 | `tests/hero-curve.test.ts` |
| 증강이 역전 담당 | 무증강 Lv30 < GOD 1.5기 | 통과 | `tests/hero-curve.test.ts` |
| 사망 라운드 (봇) | R55~66 (R60 종결 정합) | 중앙값 R60 | 시뮬 (재측정 시 유지 확인) |
| 최적 영웅 빌드 딜 지분 | 40~55% | **미측정** (2안 개편 후) | 수동 시뮬 — 과제 |
| 재화 배분 수렴 | 단일 정답 없음 | 프로브 우위 +2R | 시뮬 |
| 한 판 시간 | 20~25분 | R60 = 22분 (종결 구현 시) | — |
| 경험 지표 (이해도·부당감 등) | — | **미측정** (플레이테스트 진행 중) | — 과제 |

목표를 벗어난 지표는 §12 미해결 과제로 올린다.

## 11. 설계 결정 기록 (왜 이렇게 됐나)

| 결정 | 이유 | 상태 | 검증 |
|---|---|---|---|
| 라운드 고정 간격 | 클리어 진행식은 보스 쿨 악용을 만든다 | 채택 | — |
| 5라운드 사이클 난이도 | 지수를 구분선형으로 — 예측 가능 + 주기적 긴장 | 채택 | — |
| 영웅 타입/전직 **삭제** | 정보 0의 시작 선택 → Lv5 전직 → 최종적으로 드래프트 관성으로. 선택이 맥락을 갖는 지점은 증강 선택 그 자체다 | 채택 | — |
| 등급의 몹 체력 대가 **삭제** | 보너스는 약한 축(영웅), 비용은 강한 축(타워)에 얹혀 GA가 탐욕 0으로 수렴 — 구조적 함정이었다 | 채택 | — |
| 골드 강화(×1.12) → 3스탯 구매 | 단일 트랙은 "사냐 마냐"뿐. 스탯 3종 + 성장 곡선으로 골드 운영에 빌드가 생겼다 | 대체됨(영웅 성장 2안) | — |
| 체력 레벨 기울기 분리(1.0 < 2.4) | 힘몰빵 탱커가 부술 수 없는 벽(R114)이 됐다 | 채택 | — |
| 보스 저지 불가 | 어그로 110 이후 무한 저지로 보스 실패 모드가 소멸 — 소환이 공짜 소득이었다 | 채택 | — |
| 가스 싱크 3종 경쟁 | 프로브→가스→업글이 통째로 무뇌 루틴이었다 (GA 전 세대 수렴) | 채택 | — |
| 리롤 상한 2회 | 무제한이면 플래티넘까지 굴리는 노동이 된다 | 채택 | — |
| 스탯 비용 기울기 14 | 후반 포인트 총량 제한 — 영웅몫 82% 과열의 해법은 포인트 경제였다 (레벨 기울기 조정은 구매 포인트가 같은 곱을 타서 역효과) | 대체됨(영웅 성장 2안) | — |
| 유닛 생성 비용 누적 증가 (12 → `12 + 0.45×max(0, n−8)`) | 고정 12는 중반 미네랄의 94%를 빨아들여 GOD 20기(필드의 60%)·타일 40칸 포화(R61)를 만들었다. 포화 순간 타워 DPS 성장이 멈춰 절벽이 생긴다. 처음 8기를 12로 두는 이유는 초반 전력 보존 — 오프셋 없이 걸었더니 75미네랄로 6기를 못 사 Lv1 보스 처치율이 0.95→0.80으로 떨어졌다 | 채택 | [진단 리포트](../reports/survival-curve-diagnosis-v0.1.md) · GOD 1기 R28·5기 R55·R51에 4.5기 (이전 R20/R32/20기) · 타일 포화 소멸 · `boss-balance.test.ts` 통과 |
| 웨이브 총체력 = 목표clear × 기대DPS (사이클 HP점프 폐기) | R30까지 난이도 상승이 체감 안 됨(플레이테스트) — 사이클 안 평평한 HP를 초반 ×1.23/R로 크는 플레이어 DPS가 앞질렀다. 시뮬 측정 유효 DPS(P50)에 목표 clear(R1 18초 → R44 37초 → 지수 벽 ×1.2)를 곱해 역산. 벽이 지수인 이유: 선형 벽은 상대성장률이 감소해 지수 보드가 이긴다(봇 R138 생존) | 채택 | 사망 중앙값 R60·분포 R55~66 (시드 24), R1~3 라이프 -4, 몹 동시 17~28기 |
| 몹 수 20~36 + XP_PER_MOB 0.65 | 밀도 요청(플레이테스트). count는 난이도가 아니라 밀도다(총체력은 모델이 역산). XP는 라운드당 총량 보존 — 안 하면 30레벨 각성 창(R30~35)이 깨진다 | 채택 | hero-curve 테스트 유지 |
| 프로브 30/1.3 → 100/1.5 | "무조건 프로브 1순위 = 재미없다". 30은 오프닝 구매 가능 + 생존 우위 +8R의 지배 전략이었다. 100이면 오프닝 불가(시작 55), 우위 +2R | 채택 | 전략별 사망 중앙값 R58 vs R60 (시드 12) |
| 보스 HP ×2.15 → ×2.5, 보상 [5,10,18,32,55,90] [프로토], Lv3까지 접촉 무해 | "항상 최고 소환 = 항상 승리"(Lv6 처치율 96%). 원본 보상표(trigger #601~#606)는 리스크를 따라가지 못했다. 저레벨 보스는 소득원 — 위협은 누출 라이프(2+L)만 | 채택 | 처치율 Lv4 73% · Lv5 54% · Lv6 75% (항상 최고 소환 봇) |
| **영웅 성장 2안** — 레벨 배수 폐지, 골드→XP(20골드=20XP)→레벨업 focus 택1 | 레벨 배수(×2.4/L)가 골드 없이 킬 XP로 올라 파워커브 과속(Lv17 DPS 985). 스탯 골드 구매와 XP 구매를 병렬로 두면 과복잡 — 한 축으로 통합. 1안(레벨 한도 후 스탯)은 축을 순차 배열할 뿐이고 만렙 전 빌드 선택이 사라져 기각 | 대체됨(영웅 성장 3안) | [설계](../exec-plans/hero-progression-redesign.md) · 골드 저울·레벨 속도 앵커 (`hero-curve.test.ts`) |
| **영웅 성장 3안** — 레벨업 스탯 선택 삭제, 세 스탯 자동 균등 성장, XP 구매 유지 | 랜덤 타워의 약점을 증강으로 보정하는 도중 기존 focus 투자 때문에 영웅 빌드를 틀기 어려웠다. 리셋 UI를 추가하지 않고 선택 잠금을 제거한다. 총 포인트를 3등분해 파워 총량은 보존하며, 골드는 타워·프로브·XP 사이에서 계속 경쟁한다. 직접 스탯 구매는 후반 미네랄 잉여가 측정될 때만 검토 | 채택 | 자동 균등 성장·선택 대기 제거 테스트 통과. 수치 밸런스는 시스템 확정 후 재측정 |
| XP_PER_MOB 0.65 → 0.3 | 킬 XP를 부축으로 — 주 연료는 골드 구매. 성장이 공짜면 골드 저울이 깨진다 | 채택 | R45 킬 XP만으론 Lv25 미달 테스트 |
| **증강 계열을 기능축으로 재편** (탱커/원거리/마법사/스탯/유틸 → 강화/방어/특수/스킬/성장/경제/유틸) | 우리 증강 26종을 LoL 아레나·TFT와 같은 기준으로 분류해 보니 **스탯 강화가 31%**였고 `성장`·`시너지`는 0%였다. 계열이 빌드 원형(탱커/원거리)이라 "무엇을 하는 증강인가"가 풀 구성에 드러나지 않았고, 그래서 스탯 편중이 보이지 않았다. 계열을 기능으로 바꾸면 특화(3개)/대특화(5개)가 곧 플레이 스타일이 된다 — 성장에 몰면 눈덩이, 경제에 몰면 부자 | 채택 | [증강 분류 레퍼런스](../reference/augment-taxonomy-v1.0.md) · `augments.test.ts`가 스탯 계열 ≤20%·계열별 ≥9종을 강제 |
| **증강 풀 26 → 72종**, 발동 효과 프리미티브 신설 (치명타·처형·화상·감속·가시·폭사·흡혈·성장 누적·라운드 수입·어그로 범위·타워 사거리) | 스탯 비중을 낮추려면 **다른 카테고리에 실제로 넣을 것이 있어야** 한다. 아레나의 중심축은 특수 능력(35%), TFT의 중심축은 경제(43%)인데 우리는 둘 다 사실상 비어 있었다. 데이터만 늘리는 것으로는 안 되고 게임 로직에 효과 훅이 필요했다 | 채택 | 스탯 계열 14% (10/72) · `augments.test.ts` 25개 · **GA 시뮬(quick) 사망 p10/p50/p90 = 56/60/63 — 재편 전(중앙 R60·R55~66)과 동일, 생존 곡선 유지** |
| **대가형(리스크) 증강 12종** 도입, 단 **등급은 대가를 키우지 않는다** | 아레나 증강의 재미 상당 부분이 도박성(체력을 깎고 강해진다)인데 우리 풀엔 없었다. 다만 대가를 등급에 비례시키면 "높은 등급이 뜬 게 손해"인 경우가 생겨 §7의 **등급 = 대가 없는 순수 뽑기 운** 원칙이 깨진다. 그래서 이득만 등급 배수를 타고 대가는 고정이다 | 채택 | `augments.test.ts` — 플래티넘 광전사는 공격력만 오르고 체력 대가는 실버와 동일 |
| 성장 스택은 **영웅 막타만** 계산 / 처형은 **보스 제외** / 폭사는 **연쇄 금지** | 셋 다 폭주 방지 장치다. 타워 막타까지 세면 성장이 영웅 빌드와 무관해지고, 보스에 처형이 걸리면 소환 즉시 증발하며, 폭발이 막타를 치면 무한 연쇄가 된다 | 채택 | `augments.test.ts` 각각 테스트 |
| **영웅 기본 체력 260 신설** · 몹 이동속도 52 → 42 | 초반이 너무 어려웠다. Lv1 체력 140에 몹 10기가 붙으면 R1에 8.8초 만에 죽었고(라운드 22초), 시드 12판 중 9판에서 영웅이 죽었다(첫 사망 중앙값 R5). 기본 체력은 초반만 두껍게 하고 후반 비중은 줄어든다. 몹 감속은 타워 사격 기회를 늘려 체감 템포를 낮춘다 — **타워 공격속도를 낮추는 안은 기각**했다(몹 체력이 기대 보드 DPS에서 역산되는데 그 곡선은 코드 상수라, 타워만 느려지면 오히려 더 어려워진다) | 채택 | R5 이전 영웅 사망 **6/12판 → 0/12판** · GA 사망 p50 R60 → **R63**(누출 감소분) |
| **화상을 중첩·점화·증폭 구조로** — 화상은 **방어력 무시(트루 피해)** | 화상이 "때리면 초당 고정 피해"라 아무 선택도 만들지 않는 죽은 카드였다. 이제 때릴수록 중첩되고(공속·다단히트와 맞물림), 최대 중첩은 '점화'로 터뜨릴 자원이 되며, 불바다 장판·레이저 틱도 중첩을 쌓는다. 트루 피해라 장갑(라운드마다 +3 계단)이 오르는 후반에 정체성이 생긴다. **화염 한 장은 완력 한 장보다 약하다**(×1.15 vs ×1.70) — 씨앗이고, 쌓아야 보상이 나온다 | 채택 | 측정: 화염 1장 무증강 대비 ×1.15 · 화염빌드(부착·불쏘시개·점화) ×21 · `augments.test.ts` 화염 6개 |
| 몹·보스 **클릭 시 스탯 표시** | 몹이 얼마나 아픈지·몇 대에 죽는지 알 방법이 없었다. 체력·장갑·이동속도·접촉 공격력에 더해 "영웅이 몇 초 버티는가 / 몇 대에 잡는가"를 함께 보여준다 — 플레이어가 어그로를 걸지 말지 판단할 근거 | 채택 | `selectedEnemy` · `enemyInfo()` |
| **쿨타임 폐지 → 마나 (TFT식)** — 평타(+10)·피격(+8)으로 차고, 가득 차면 자동 시전 | 쿨타임은 스탯과 아무 관계가 없어서 스킬이 빌드에서 떠 있었다. 마나로 바꾸면 **공속이 곧 스킬 회전**이 되고(민첩 투자에 이유가 생김), **맞으면 마나가 차서 탱커도 스킬을 쓴다**(어그로가 자원이 됨). '맞으면 마나 회복'을 쿨감으로 번역하던 군더더기도 사라진다 | 채택 | 필요 마나 하한 40% (없으면 난사 빌드가 강화 빌드를 10~40배 압도) · GA 사망 p50 R62 |
| 스킬을 **단발/채널링**으로 나눔 — 채널링은 지속 중 **마나가 차지 않는다** | 마나 감소를 무한정 쌓으면 모든 스킬이 상시 발동이 된다. 채널링(레이저·허수아비)은 지속 중 마나가 멎어 가동률에 천장이 생기고, 단발은 마나가 차는 대로 계속 터진다 — 그래서 "마나 증강을 몇 장 집을 것인가"의 답이 스킬마다 달라진다 | 채택 | `augments.test.ts` 채널링/단발 3개 |
| **화상 재설계** — 평타로 안 붙고 **스킬·도트만** 불을 붙인다 / 중첩 상한 없음 / 겹당 **고정 피해**(공격력 계수 없음) / **방어력 무시** | 화상이 "때리면 초당 고정 피해"라 아무 선택도 만들지 않았다. 이제 화염은 평타 빌드의 부록이 아니라 **스킬·도트 빌드의 심장**이다 — 레이저는 틱마다, 불바다는 초마다 겹을 얹으므로 마나 회전이 빠를수록 두꺼워진다. 성장축이 "얼마나 세게"가 아니라 **"얼마나 여러 겹"**이 된다 | 채택 | 측정: 화염 1장은 완력 1장보다 약하다(씨앗) · 난사 화염 빌드 최대 100겹+ |
| **점화의 폭발 메커니즘 삭제** → 화염 피해 배수로 | 점화는 "최대 중첩에 닿으면 겹을 태워 폭발"이었다. 즉 **중첩 상한이 있어야만 성립하는 장치**였는데 상한을 없애자 전제가 사라졌다. 폭발은 오히려 애써 쌓은 겹을 0으로 되돌려 해로웠고, 도트가 겹을 계속 갱신하면 아예 안 터졌다 | 채택 | 점화 = 화상 겹당 피해 + 불바다 피해에 곱해지는 배수 |
| 맹독을 딜 → **약화**로 (감속 + 방어력 감소) | 화상과 역할이 겹쳤다. 방깎은 **타워 피해도 함께 이득**을 보므로 영웅이 보드를 돕는 축이 된다 | 채택 | `augments.test.ts` |
| **튕기는 사격** 신설 — 튕길수록 강해지고(×1.3, 5회) 몹이 적으면 약하다 | "언제 쏘는가"가 곧 실력인 스킬. 처음엔 ×1.7·6회로 뒀더니 마나 난사와 곱해져 기준 대비 ×43(같은 장수 강화 빌드는 ×1.83)까지 갔다 — 측정 후 하향 | 채택 | 측정: 몹 6기에서 난사 풀빌드 ×5.8 (강화 9장 ×2.45) |
| 초신성을 **체력 계수** 중심으로 (공격력 ×2 + 최대 체력 ×0.4) | 딜러의 폭탄이 아니라 **탱커의 마지막 한 방**이어야 한다. 체력 계수가 공격력 계수보다 크다 | 채택 | — |
| **대재앙 하향** (반경 +35·스킬 피해 +30% → 반경 +30·+15%) | 한 카드가 **두 곱셈 축을 동시에** 키웠다 — 반경은 대상 수를, damageMult는 한 방을 키우고 둘이 곱해진다. 그래서 곱연산 목록에 없는데도 실질 곱이었다(§7 "곱은 소수 정예"와 충돌). 게다가 대재앙 2스택이면 유성과 합쳐 스킬 계열 3장이 되어 **각성 시너지가 공짜로 딸려왔다**. 결과적으로 유성 빌드의 정답이 하나뿐이었다. 파워를 카드에서 걷어내 **시너지 쪽으로 옮긴다** — 스킬 계열을 5장까지 채워 대특화(초월 시전)를 켜야 크게 강해지도록 | 채택 | 아레나 측정(Lv35·5슬롯 고정·더미 40기): 대재앙2 단독 **×2.32 → ×1.98**(비교군 증폭2는 ×1.72), 시너지까지 채우면 ×3.68 — **시너지 이득 ×1.72 → ×1.86**. `skills.test.ts` 3개 |
| 대재앙의 **화상 연계는 기각** | "유성이 화상을 여러 겹 얹는다"로 화염 드래프트를 강제하려 했으나, 유성은 마나 160짜리 버스트라 **8초에 한 번** 나가는데 화상은 3초면 꺼진다. 겹이 쌓이질 않아 화상 기여도가 총딜의 3~6%(16k/450k)에 그쳤다. **화염의 연료는 도트여야 한다**(레이저 틱·불바다 장판) — 버스트 스킬은 불을 지필 수 없다 | 기각 | 측정 후 되돌림 (최대 겹 0) |
| **영웅 비전투 재생** 신설 — 마지막 피격 후 4초가 지나면 초당 최대 체력 6% | 회복 수단이 재생·흡혈 증강, 레벨업, 증강 획득뿐이라 **회복 증강을 안 뽑으면 깎인 체력이 영영 안 돌아왔다**. 결국 죽어서 부활하는 게 유일한 회복이 됐다 — "죽는 게 회복"은 나쁜 루프다. 영웅은 경로 위에서만 움직이므로 이건 곧 **"물러나면 회복한다"**가 되어, 이동이 유일한 직접 조작인 게임에서 후퇴·복귀가 전술이 된다. 회복률이 최대 체력 비례라 탱커일수록 절대량이 크다. 재생 증강은 여전히 값어치가 있다 — 그쪽은 **맞는 중에도** 찬다 | 채택 | 풀피까지 ~17초(라운드 간격 22초) · `hero.test.ts` 5개 |
| **증강을 곱연산 → 가산 합산으로** (곱은 3종만: 광전사·진화·과부하) | 전부 곱이면 스택형 증강이 복리로 터진다. 측정: 성장 3장이 R40에 GOD **14.7기**(R44 보드 전체 5.1기)를 냈고, 완력 3장 × 특화도 ×4.3이었다. 가산이면 증강 한 장의 값어치가 예측 가능해지고 후반 폭주가 사라진다. 곱을 소수만 남기고 배율을 낮추면(×1.4~1.6) 곱이 **사건**이 된다 | 채택 | 성장 3장 14.7기 → **1.9기** · 공격 3증강(Lv24) GOD **1.07기**(앵커 1~2.5) · `hero.test.ts` 가산/곱 테스트 |
| **발동 효과를 고정값 → 영웅 공격력 비례로** (화상·장판·폭사·사망폭발) | 화상이 초당 8 고정이었는데 Lv30 영웅 공격력이 614이라 사실상 0이었다. 고정 수치는 레벨을 못 따라가 특수 계열이 통째로 죽은 카드였다(측정: 특수 3장 = 무증강과 동일 DPS) | 채택 | 특수 3장 0.33기 → **0.9기** |
| **경제를 고정 가산 → 라운드 보상 배수로** | 라운드 보상이 `10+3R`(R30=100)인데 '수확'은 라운드당 +6이었다 — 6%짜리였다. 배수여야 라운드를 따라 큰다 | 채택 | `waveRewardMult` · `augments.test.ts` |
| **스킬 5종 추가** (레이저·불화살·얼음화살·처형자의 일격) + 지형 장판 프리미티브 + 공용 개조(한파·다중 투사·반격 집중) | 스킬이 4종뿐이라 스킬 계열이 얇았다. 원형별로 채운다: 근접(소용돌이·처형) / 원거리(일제사격·불화살·얼음화살) / 마법(유성·레이저) / 탱커(허수아비). 장판은 "몹이 지나갈 수밖에 없는 길목에 놓는 지속 효과" — 1D 경로 모델과 잘 맞는다. 마나가 없으므로 '맞으면 마나 회복'은 **피격 시 쿨감**으로 번역했다 | 채택 | `augments.test.ts` 스킬 확장 11개 |
| 허수아비·처형은 **공속이 쿨을 깎는다** | 탱커가 민첩에 투자할 이유가 없었다. 손이 빠르면 미끼도 자주 세운다 | 채택 | `resolveSkill(attackSpeedRatio)` |
| **타워 복제** — 라운드 중 타워를 찍으면 라운드 종료 시 복제. 복제 가능 티어 상한은 라운드·영웅 레벨을 따라 오른다 | 영웅 증강이 영웅만 강화하면 타워 보드와 겹치지 않는다. 복제는 **생성 비용을 올리지 않아서**(unitsSpawned 불변) 값어치가 크고, 티어 상한이 계속 올라 "지금 뭘 베낄 수 있나"가 매 라운드 갱신된다. 복제본이 원본과 같으면 즉시 조합된다 — 그게 티어 점프의 지름길 | 채택 | `augments.test.ts` 타워 복제 4개 |
| **초반 느린 템포** (2026-07-16) — 전투 스텝에만 `combatDt = dt × earlyTempo(round)`(R1 0.6→R5 1.0), 몹 HP도 ×p로 낮춤 | "게임 초반을 느린 템포로 시작하고 싶다"(사용자 요청). 몹·보스·영웅 이동, 타워·영웅 공속, 투사체·장판·화상이 통째로 같은 비율로 느려지는 **순수 슬로우모션**이라 전투 자체의 난이도는 수학적으로 불변. 라운드 타이머·스폰·가스·보스 쿨다운은 실시간 `dt`라 라운드 진행 속도는 유지. **타워만 늦추는 안은 과거 기각**(위 §영웅체력260 행 — 몹 HP가 보드 DPS 역산이라 타워만 느리면 더 어려워진다)됐는데, 전투 전체를 같은 비율로 늦추면 그 함정을 피한다. 몹 HP ×p는 라운드 타이머를 안 늦춰 생기는 웨이브 누적 압력을 상쇄 + 초반 완화 — 이 한 축만 튜닝 대상 | 채택 | GA(quick) 사망 p10/p50/p90 = **53/64/69** — 죽는 지점이 전부 tempo=1.0(R5+) 영역이라 중후반 난이도 불변. 보스전은 순수 슬로우모션(HP·속도 원본 유지)이라 `boss-balance.test.ts` 포함 **전체 279 테스트 통과** |
| **경제·파워커브 재조정** (2026-07-16) — ① 가스 업그레이드 복리 ×1.1^L → **가산 1+0.4L**, 비용 8+4L+L² → **선형 2+4L** ② 기본공 7 → **4**, 장갑 절반(몹 스텝 3→1.5, 보스 3L→1.5L, 보스 HP 1150→700) ③ XP 성장 지수 1.06 → **1.10** ④ 프로브 진입가 100 → **60** ⑤ 웨이브 곡선 재적합 + 종반 벽 R44→R40 | 플레이테스트: "최소 타워 + 영웅 몰빵"이 지배 전략 — 기준선 측정으로 확정(골드 70%를 영웅에 부으면 R25 영웅 DPS가 필드 최강 타워의 **×15.7**, 생존은 타워몰빵과 동급. XP는 20골드 고정인데 타워 비용은 라운드마다 상승). 가스 엔진은 죽어 있었다(R60 업그레이드 4회, 첫 업 8 > 시작 가스 6). 처방: 공짜 파워(기본공)를 깎아 구매 파워(가스 업)로 이관 — 가산+선형 조합만이 "레벨이 오를수록 가스 효율 감소"를 만든다(복리+선형은 몰빵 폭주). 장갑은 감산이라 원피해와 함께 절반. XP 지수 1.10은 골드 2배당 +7레벨(1.06은 +12)로 영웅 골드의 한계 효용을 꺾는다 | 채택 | [계획·기준선](../exec-plans/economy-power-rebalance.md) · 영웅몰빵 지배 전략 소멸. 가스 업그레이드 R60 4회 → **28회**(봇 프로브 3기; 프로브 몰빵 시 40+ 예산). 보드 후반 성장률 3.5% → **7.1%/라운드**. 사망 중앙 R58~60 유지 |
| **난이도 곡선 킥 폐지 — 성장률 연속 램프 + 클리어율 목표** (2026-07-16 3차) — 목표 clear의 선형→지수 전환점(R40에서 성장률 1.3%→20% 점프) 폐지 → 성장률이 R30~48에 선형으로 1.4%→20%(연속 복리) 오르는 로그-2차 램프, 이후 유지 | 사용자 지시: **최종 보스는 없다** — R60도 그냥 강한 웨이브이므로 "갑자기 높아진 난이도를 벽으로 느끼지 않아야" 한다. 목표: R60 도달 ≤10%, 클리어 ≤5%. 함정: 매끈함만 좇아 후반 성장률이 보드 성장(+7.1%/R) 밑으로 깔리면 R50 생존 보드가 곡선을 따돌리고 영생한다 — 후반 상한(20%)은 반드시 보드 성장 위 | 채택 | 시드 16×정책 7 = 112판: **도달 6.3% · 클리어 5.4%** · 사망 중앙 R46~55 · 영웅몰빵 도달률 최고(12.5%) = 고위험 빌드의 고점이 가장 날카로움. `game.test.ts` 불변식: 후반 성장 ≥+10%/R · 성장률 단조 · 킥 금지(증가폭 ≤2%p/R). 전체 280 테스트 통과 |
| **5차 — 페이스 미세조정 + 보스 Lv7 + 조기 증강** (2026-07-17) — 생성비 증가 0.35→**0.40** · 업 비용 2+4L→**2+6L**(효과 0.45 유지, 페이스만) · **보스 Lv7 신설**(보상 150, 무해, HP 550×3.4⁶=1.1M — 원본은 Lv6까지 [프로토]) · 증강 첫 둘 **Lv5·10** 조기화 · 종반 상한 0.27 | 플레이테스트: "GOD 페이스 10~20%만 감속 / 업그레이드로 너무 빨리 강해져 라운드 지루 / R30 전 Lv6 클리어 시 성장 흥미 소멸 → Lv7 / 첫 증강 2개는 빠르게 — 증강 보고 빌드 결정" | 채택 | 사다리 Lv4 R22 → Lv5 R35 → **Lv6 R44(절반) → Lv7 봇 미답**(사람 고점) · 도달 8.9% · 클리어 3.6% ✓ · GOD R40 3기→R45 4기 · 291 테스트 통과 |
| **4차 — GOD 다수 체제·가스 주축·영웅 후반 가치** (2026-07-17) — 기본공 4→**3**(장갑 1.1 동행) · 가스 0.25→**0.4/s** · 업그레이드 가산 0.4→**0.45** · 생성비 증가 0.45→**0.35** · 라운드 보상 R35+ 이차항 · XP 지수 1.12→**1.10** + 스탯 1+L/7→**1+L/6** · 보스 550×**3.4**^L · 곡선 재적합(후반 8.8%/R) + 램프 52/0.25 | 플레이테스트 5건: GOD 1기+3~4업이면 R20에 Lv6 → R20~40 공짜 / R40 절벽 + 선형 보상 / 영웅 Lv32에 4천 골드 넣어도 타워가 월등 / 생성비 급등 체감 / "기본공 더 깎고 가스 올려 광부 2~3+타워만으로 R40 GOD ~4기". 파워를 공짜(기본공)에서 구매(가스 업·GOD 다수)로 더 이동하고, 영웅은 비용 완화+후반 스탯으로 골드 효율 ≈×1.9 회복 | 채택 | [계획 §8](../exec-plans/economy-power-rebalance.md) · 사다리: Lv4 R20 → Lv5 R31 → **Lv6 R40**(시도 8회) · 도달 7.1% · 클리어 4.5% ✓ · GOD(타워몰빵) R40 3기→R45 4기 · 몰빵 영웅/최강타워 R40~45에 1.2~2.6, 균형 0.35~0.5 · 291 테스트 통과 |
| **보스 사다리 재조정 + 리스크 단순화** (2026-07-17) — 보스 HP 성장 2.5 → **3.0** (base 700 유지, Lv6 68k → 170k), 접촉 무해 Lv3 → **전 레벨**, 누출 라이프 2+L → **-1**(잡몹과 동일), 종반 벽 상한 0.20 → 0.22 | 플레이테스트: "R20~25에 Lv4 타워만으로 보스 4·5, GOD 1기면 Lv6까지 — 이후 타워 건설 무의미". 사다리 측정(항상 최고 소환 봇, 시드 12): Lv4 첫 클리어 중앙 **R15** · Lv6 R22 — 소진이 너무 빨랐다. 보스 리스크는 한 겹으로 통일: **못 잡으면 쿨타임(45초) 동안 처치 보상을 못 얻는 기회비용** + 라이프 -1. 접촉 피해·라이프 폭탄(2+L)을 걷어내 공격적 소환을 장려하고, 실패는 시간으로 갚는다. 무해화로 후반이 물러진 만큼(클리어율 11.6%) 벽 상한을 되조임 | 채택 | 성장 3.0에서 Lv4 R17 10/12(실패 리스크 생김) · Lv5 2/12 · **Lv6 0/12**. 후반 산술: R40+ 보드(3~6k DPS)가 노출 1회에 Lv6 가능 — 사다리가 종반 콘텐츠로 이동. R60 도달 9.8% · 클리어 6.3% (목표 ≤10%/≤5%) · 291 테스트 통과 |
| **빌드 다양성 철학 확립 + 영웅 개화 지연** (2026-07-16 2차) — 판정 기준을 "생존 평준화(±3R)" → **"각 빌드의 고점(p90)이 클리어 라인에 닿고, 지배 전략만 금지"**로 재정의. XP 지수 1.10 → **1.12**, 스탯 포인트 2+L/10 → **1+L/7**(백로딩 — 중반 -20%, 후반 보존), 타워 지휘 증강 상향(전쟁군주 +12→20% 등) | 사용자 철학: "여러 빌드가 있고 각 고점이 클리어 가능하면 좋겠다. 리스크 프로필은 달라도 된다(고위험·고고점 vs 안정·저고점). 여러 방식으로 클리어하는 재미가 가장 중요. 막을 것은 특정 빌드가 너무 쉬워서 다른 빌드가 죽는 것뿐." + "영웅이 타워보다 강해지는 건 R45 이후, 타워 증강 2장 이상이면 끝까지 타워 우위". 백로딩은 영웅을 늦게 피우되 고점(클리어력)은 보존한다 | 채택 | 시드 8×정책 7: 영웅몰빵 18~30/50~54/60~67(고위험·고점 클리어권) · 균형 50/58/69 · 타워몰빵 49/59/64 — **전 빌드 p90이 R60에 닿고 지배 전략 없음**. 균형 빌드의 영웅/최강타워 비율 0.3~0.7(영웅이 안 넘음), Lv30 도달 R50대(개편 전 "Lv30 = GOD ×3" 소멸). 타워증강 2장 앵커는 `hero-curve.test.ts`가 고정. 전체 280 테스트 통과 |

## 12. 미해결 과제

0. **유한 종결 R60 — 곡선은 맞췄고 종결 자체가 미구현.**
   [설계 문서](../exec-plans/finite-ending-design.md). 웨이브 재설계(§2.1)로 무적 중반은
   해소됐고 봇 사망이 R55~66에 온다. 남은 것: **R60 최종 보스·종결 연출**(지금은 그냥
   계속 어려워질 뿐 "끝"이 없다), R51~60 증강 차단, **증강 레벨 재배치**(5·6번째
   Lv35·42는 감속된 레벨 곡선에서 재검토 필요 — 2안 개편으로 도달 레벨이 바뀌었다).
   목표 생존곡선(R45 30%·R60 클리어 1%)은 사람 플레이테스트로 확정.
1. **개편 후 빌드 재측정** — 영웅 3안 + 새 웨이브에서 영웅몫·빌드별 생존 스프레드·
   탱커 어시스트·원거리/중반 공백(구 §12 1~3번)을 전부 다시 재야 한다.
   구 측정치는 §10에서 폐기됨.
2. **판 길이** — R60 종결 구현 시 22분로 목표(20~25분) 안. 종결 전까지는 열려 있음.
3. **빙고/미션 시스템** — 수입의 두 번째 결정 축.
   [income-curve.csv](../balance/income-curve.csv)가 근거: 수입 격차를 만드는 선택이
   보스 레벨 하나뿐이다. [백로그](backlog.md) 승격 항목 참조.
4. **몹 2~3열 레인 렌더** — 경로 폭 확장 + 시각 밀도 (웹 render + Unity View).
5. **Unity 런타임 검증** — 자동 균등 성장 표시·커맨드 카드·XP 구매의 에디터 실측 미수행.

## 13. 부록 — 새 시스템 기록 체크리스트

새 시스템을 명세에 추가할 때 아래를 채운다 (전부 강제는 아니고 체크리스트):

- **개발 의도** — 왜 존재하는가
- **강화하는 필러** — [vision.md](vision.md)의 P1~P4 중 무엇
- **플레이어의 결정** — 무엇을 보고 무엇을 고르는가
- **규칙** — 실제 동작
- **피드백** — UI·이펙트로 결과를 어떻게 알리나
- **밸런스 레버** — 조정 가능한 값과 코드 위치
- **성공 지표** — 어떤 측정값이면 의도대로인가 (§10.5에 등록)
- **예외·경계** — 중첩, 사망, 판매, 대상 소멸 처리

## 14. 밸런스 시트 관리 방침

- **원본은 코드다** (`web/src/data/*.ts`). 사람이 시트를 고치지 않는다.
- `npm run gen:balance` → `docs/balance/*.csv` 7종 자동 생성.
  `tests/balance-csv.test.ts`가 코드-시트 일치를 강제한다 (어긋나면 CI 실패).
- 외부 공유(구글 시트 등)는 **읽기 전용 사본**으로 올린다. 시트에서 수치를 바꾸고
  싶으면 코드를 바꾸고 재생성한다 — 역방향 동기화는 하지 않는다.
